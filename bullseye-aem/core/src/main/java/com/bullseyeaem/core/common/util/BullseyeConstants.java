package com.bullseyeaem.core.common.util;

public class BullseyeConstants {
    public static final String FIXED_WIDTH = "fixedWidth";
    public static final String SORTABLE = "sortable";
    //TODO: Replace this constant with a programmatic lookup of `configPath` defined in the `Apache Sling Context-Aware Configuration Default Resource Resolving Strategy` OSGI config.
    // Possibly use one fo the classes in org.apache.sling.caconfig
    public static final String CONF_ROOT = "/conf";
}
